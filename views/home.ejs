<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Join Random Chat</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    body {
      background: linear-gradient(to right, #f0f8ff, #d0e8ff);
      display: flex;
      min-height: 100vh;
      overflow-x: hidden;
      animation: fadeInBody 1s ease-in-out;
    }

    @keyframes fadeInBody {
      0% {
        opacity: 0;
      }

      100% {
        opacity: 1;
      }
    }

    .container {
      display: flex;
      width: 100%;
      animation: fadeInContainer 1s ease-in-out;
    }

    @keyframes fadeInContainer {
      0% {
        transform: translateY(40px);
        opacity: 0;
      }

      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .left-section {
      flex: 1;
      background: linear-gradient(to bottom right, #007bff, #00bfff);
      color: white;
      padding: 60px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      animation: floatUpDown 6s ease-in-out infinite;
    }

    @keyframes floatUpDown {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-10px);
      }
    }

    .left-section h1 {
      font-size: 36px;
      margin-bottom: 20px;
      transition: all 0.3s ease;
    }

    .left-section p {
      font-size: 18px;
      margin-bottom: 30px;
      transition: all 0.3s ease;
    }

    .left-section ul {
      list-style: none;
      padding-left: 0;
    }

    .left-section li {
      margin: 10px 0;
      font-size: 16px;
      transition: transform 0.3s ease;
    }

    .left-section li:hover {
      transform: translateX(8px);
    }

    .left-section i {
      margin-right: 10px;
    }

    .right-section {
      flex: 1;
      padding: 60px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      opacity: 0;
      animation: fadeInRight 1.2s ease forwards 0.5s;
    }

    @keyframes fadeInRight {
      0% {
        transform: translateX(50px);
        opacity: 0;
      }

      100% {
        transform: translateX(0);
        opacity: 1;
      }
    }

    .right-section h2 {
      font-size: 28px;
      color: #007bff;
      margin-bottom: 20px;
      transition: color 0.3s ease;
    }

    .right-section h2:hover {
      color: #0056b3;
    }

    .btn {
      background: #007bff;
      color: white;
      padding: 15px 40px;
      font-size: 18px;
      border: none;
      border-radius: 50px;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .btn i {
      margin-right: 10px;
    }

    .btn::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      width: 0%;
      height: 100%;
      background: #0056b3;
      z-index: -1;
      transition: 0.4s;
      border-radius: 50px;
    }

    .btn:hover::before {
      width: 100%;
    }

    .btn:hover {
      color: #fff;
      transform: scale(1.05);
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
      }

      .left-section,
      .right-section {
        padding: 40px 20px;
        text-align: center;
        align-items: center;
      }

      .left-section h1 {
        font-size: 28px;
      }

      .left-section p,
      .left-section li {
        font-size: 16px;
      }

      .right-section h2 {
        font-size: 22px;
      }

      .btn {
        width: 100%;
        padding: 14px 0;
        font-size: 16px;
      }
    }

    @media (max-width: 480px) {
      .left-section h1 {
        font-size: 24px;
      }

      .right-section h2 {
        font-size: 20px;
      }

      .btn {
        padding: 12px 0;
        font-size: 15px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="left-section">
      <h1><i class="fas fa-video"></i> Video Chat App</h1>
      <p>Connect with new people instantly. Join a random video chat room and start talking!</p>
      <ul>
        <li><i class="fas fa-random"></i> Random Room Matching</li>
        <li><i class="fas fa-video"></i> High Quality Video</li>
        <li><i class="fas fa-shield-alt"></i> Private & Secure</li>
      </ul>
    </div>

    <div class="right-section">
      <h2>Jump into a Room with One Click</h2>
      <button class="btn" onclick="joinRandomChat()">
        <i class="fas fa-random"></i> Join Random Chat
      </button>
    </div>
  </div>

  <script>
    function joinRandomChat() {
      const roomId = Math.random().toString(36).substring(2, 10);
      window.location.href = `/room/${roomId}`;
    }
  </script>
</body>

</html>